<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Verified</title>
    <link href="https://cdn.jsdelivr.net/npm/noty/lib/noty.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/noty/lib/noty.min.js"></script>
    <link rel="stylesheet" href="/vendors/bootstrap/css/bootstrap.min.css"> <!-- Bootstrap CSS for better styling -->
    <style>
        body {
            font-family: 'Roboto', Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(to bottom, #f3f4f6, #f3f9f4);
        }

        .success-container {
            text-align: center;
            background: #ffffff;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 90%;
        }

        .success-checkmark {
            margin: 0 auto 20px;
        }

        .check-icon {
            width: 80px;
            height: 80px;
            position: relative;
            border-radius: 50%;
            box-sizing: content-box;
            border: 4px solid #4CAF50;
            margin: 0 auto;
        }

        .check-icon::before, .check-icon::after {
            content: '';
            height: 100px;
            position: absolute;
            background: #FFFFFF;
            transform: rotate(-45deg);
        }

        .check-icon::before {
            top: 3px;
            left: -2px;
            width: 30px;
            transform-origin: 100% 50%;
            border-radius: 100px 0 0 100px;
        }

        .check-icon::after {
            top: 0;
            left: 30px;
            width: 60px;
            transform-origin: 0 50%;
            border-radius: 0 100px 100px 0;
            animation: rotate-circle 4.25s ease-in;
        }

        .icon-line {
            height: 5px;
            background-color: #4CAF50;
            display: block;
            border-radius: 2px;
            position: absolute;
            z-index: 10;
        }

        .icon-line.line-tip {
            top: 46px;
            left: 14px;
            width: 25px;
            transform: rotate(45deg);
            animation: icon-line-tip 0.75s;
        }

        .icon-line.line-long {
            top: 38px;
            right: 8px;
            width: 47px;
            transform: rotate(-45deg);
            animation: icon-line-long 0.75s;
        }

        .icon-circle {
            top: -4px;
            left: -4px;
            z-index: 10;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            position: absolute;
            box-sizing: content-box;
            border: 4px solid rgba(76, 175, 80, .5);
        }

        .icon-fix {
            top: 8px;
            width: 5px;
            left: 26px;
            z-index: 1;
            height: 85px;
            position: absolute;
            transform: rotate(-45deg);
            background-color: #FFFFFF;
        }

        .verified-text {
            margin-top: 20px;
            font-size: 20px;
            color: #4CAF50;
            font-weight: bold;
            line-height: 28px;
            animation: fade-in 1.5s ease-in-out;
        }

        .btn-success {
            margin-top: 20px;
            background: #4CAF50;
            border: none;
            color: #ffffff;
            padding: 12px 30px;
            font-size: 16px;
            border-radius: 30px;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s ease;
        }

        .btn-success:hover {
            background: #388E3C;
            color: #ffffff;
            text-decoration: none;
            transform: scale(1.05);
        }

        .btn-success:active {
            transform: scale(1);
        }

        .success-footer {
            margin-top: 20px;
            font-size: 14px;
            color: #777;
        }

        .success-footer a {
            color: #4CAF50;
            text-decoration: none;
        }

        .success-footer a:hover {
            text-decoration: underline;
        }

        @keyframes rotate-circle {
            0% {
                transform: rotate(-45deg);
            }
            5% {
                transform: rotate(-45deg);
            }
            12% {
                transform: rotate(-405deg);
            }
            100% {
                transform: rotate(-405deg);
            }
        }

        @keyframes icon-line-tip {
            0% {
                width: 0;
                left: 1px;
                top: 19px;
            }
            54% {
                width: 0;
                left: 1px;
                top: 19px;
            }
            70% {
                width: 50px;
                left: -8px;
                top: 37px;
            }
            84% {
                width: 17px;
                left: 21px;
                top: 48px;
            }
            100% {
                width: 25px;
                left: 14px;
                top: 45px;
            }
        }

        @keyframes icon-line-long {
            0% {
                width: 0;
                right: 46px;
                top: 54px;
            }
            65% {
                width: 0;
                right: 46px;
                top: 54px;
            }
            84% {
                width: 55px;
                right: 0px;
                top: 35px;
            }
            100% {
                width: 47px;
                right: 8px;
                top: 38px;
            }
        }

        @keyframes fade-in {
            0% {
                opacity: 0;
            }
            100% {
                opacity: 1;
            }
        }
    </style>
</head>
<style>
    #noty_layout__topRight {
      width: auto;
    }
    .noty_bar {
      width: 500px;
      height: auto; 
      display: flex; 
      align-items: center; 
      justify-content: start; 
      padding: 10px 20px;
      margin: 7px 0;
      border-radius: 5px; 
      font-size: 16px; 
      text-align: left; 
      word-wrap: break-word; 
      box-sizing: border-box;
  }
  
    .noty_type__success {
      color: green; 
      background-color: #D1E7DD; 
      border: 1px solid #A3CFBB;
    }
    .noty_type__error {
      color:#58151C; 
      background-color: #F8D7DA; 
      border: 1px solid #F1AEB5;
    }
    .noty_type__info {
      color:#055160; 
      background-color: #CFF4FC; 
      border: 1px solid #9EEAF9;
    }
    .noty_type__warning {
      color:#664D03; 
      background-color: #FFF3CD; 
      border: 1px solid #FFE69C;
    }
    .noty_type__primary {
      color:#664D03; 
      background-color: #CFE2FF; 
      border: 1px solid #FFE69C;
    }
  </style>
<body>
    <div class="success-container">
        <div class="success-checkmark">
            <div class="check-icon">
                <span class="icon-line line-tip"></span>
                <span class="icon-line line-long"></span>
                <div class="icon-circle"></div>
                <div class="icon-fix"></div>
            </div>
        </div>
        <div class="verified-text">Your Account Has Been Successfully Verified!</div>
        <a href="/admin/dashboard" class="btn btn-success">Go to Dashboard</a>
        <div class="success-footer">
            Need help? <a href="/help">Contact Support</a>
        </div>
    </div>
</body>
<script>
     if(`<%=Flash.success%>`){
       new Noty({
         text: `<i class="fa fa-check-circle" style="margin-right: 10px;"></i> <%=Flash.success%>`,
         type: 'success',
         timeout: 2500
       }).show();
     }
     if(`<%=Flash.info%>`){
       new Noty({
         text: `<i class="fa fa-info-circle"  style="margin-right: 10px;"></i> <%=Flash.info%>`,
         type: 'info',
         timeout: 2500
       }).show();
     }
     if(`<%=Flash.error%>`){
       new Noty({
         text: `<i class="fa fa-times-circle"  style="margin-right: 10px;"></i> <%=Flash.error%>`,
         type: 'error',
         timeout: 2500
       }).show();
     }
     if(`<%=Flash.warning%>`){
       new Noty({
         text: `<i class="fa fa-warning"  style="margin-right: 10px;"></i> <%=Flash.warning%>`,
         type: 'warning',
         timeout: 2500
       }).show();
     }
   </script>
</html>
